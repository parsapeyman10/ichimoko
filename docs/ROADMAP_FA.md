# نقشهٔ ساخت اپ ترید — وضعیت واقعی کد

این مخزن **اپ اندروید بومی + API پژوهشی + وب** است؛ APK موجود روی گوشی کاربر به‌صورت خودکار به‌روز نمی‌شود. نسخهٔ جدید باید ساخته و نصب شود. هیچ ادعای اتصال به حساب واقعی یا سوددهی وجود ندارد.

| درخواست | وضعیت این مرحله | محدودیت صریح |
|---|---|---|
| دیده‌بان‌های مستقل اندروید | **متصل به UI**: فارکس فقط XAU/USD؛ تب «ریالی» در فضای بورس ایران/آگاه برای دلار، طلای ۱۸/۲۴، مثقال و سکهٔ امامی/بهار/نیم/ربع/گرمی با TGJU و آینهٔ نمایشی Navasan؛ کریپتوی جهانی با نمای مستقل CoinGecko | از فارکس درخواست نمادهای ریالی/BTC/ETH/AAPL ارسال نمی‌شود. قیمت ریالی TGJU به تومان تبدیل می‌شود؛ زمان ردیف فقط ساعت است و تازگی معامله را ثابت نمی‌کند. زمان منتشرشدهٔ آینه، نه دریافت کش، معیار تازگی آن است. ساختار HTML زنده/اتصال گوشی هنوز نیازمند بررسی دستگاه است؛ چارت/سیگنال فارکس همچنان از Twelve Data است. |
| انتخاب منبع برای هر نماد و API جدا | **متصل به UI**: روشن/خاموش‌کردن منابع، انتخاب منبعِ قیمت نمایشی و کلید *خواندنی* Twelve Data به‌ازای هر نماد | کلید دیگر منابع عمومی است؛ انتخاب URL دلخواه یا کلید معاملاتی در گوشی مجاز نیست. بک‌اند پژوهشی هنوز تک‌نمادی است. |
| `CONFIRMED / CONFLICT` | **کار می‌کند** برای حداقل دو منبع مستقل، تازه، دارای زمان قیمت و واحد یکسان | یک منبع/کش/قیمت بدون زمان = `UNVERIFIED`، نبود قیمت = `NO_DATA`. مقایسه حکم صحت قطعی یا مجوز معامله نیست؛ بازار اسپات با فیوچرز مخلوط نمی‌شود. |
| کش و تاریخچه منابع | **SQLite روی دستگاه**: همهٔ مشاهدات موفق دیده‌بان از زمان استفاده، صفحه‌بندی بدون سقف شمارش؛ قیمت قدیمی با زمان خودش و برچسب کش | تاریخچهٔ *گذشتهٔ ارائه‌دهنده* بازیابی نمی‌شود؛ کش کندل چارت حداکثر ۳۰۰۰ بار در نماد/بازه است؛ خبر فعلاً فقط حافظهٔ سرور/گوشی دارد. |
| جمع‌آوری وب، تقویم اقتصادی و شرط نهم AI | تیترهای RSS/Atom ناشران در خود گوشی بدون تنظیم سرور (صرفاً نمایش و کش برچسب‌دار)، RSS مستقل در بک‌اند HTTPS برای گیت/AI، تقویم هفتگی Forex Factory با نشان قرمز رویداد پراثر در UI به‌صورت مستقیم و گیت جداگانهٔ سرور برای USD (۳۰ دقیقه پیش تا مدت توقف پس از رویداد)؛ شاهد ناشر/مدل و زمان تقویم در ژورنالِ paper. | بدون سرور HTTPS، کلید مدلِ فقط سرور، رضایت صریح/بررسی حق ناشران یا با قطع/کهنگی RSS/تقویم، حالت `UNKNOWN` و ورود سیگنالی/خودکار paper ممنوع است. تقویم تضمین پوشش کامل یا نتیجهٔ رویداد نیست؛ توقف سفارش واقعی هنوز وجود ندارد. فید اختصاصی دارای مجوز `/news/fa` جدا باقی است. |
| پایش قفل گوشی و خبر مستقل | **در APK**: watchdog اتصال فارکس با ردهٔ DELAYED، بازیابی شبکه و تازگی REST؛ opt-in فقط‌خواندنی برای کریپتو، نوبیتکس و بورس؛ رویداد High/USD Forex Factory و RSS هر فضا با توضیح قاعده‌ای/اعلان خبر پژوهشیِ مستقل و dedup. [آزمون گوشی](BACKGROUND_NEWS_FA.md). | نگه‌داشتن اتصال دائمی در Doze/Android 15 تضمین نمی‌شود؛ تقویم و RSS تحلیل مدل نیستند و جای شاهد ۹/۹ را نمی‌گیرند. خبر قدیمی/بی‌عدد جهت طلا و سفارش نمی‌سازد. |
| لانگ و شورت | **فقط paper در تب «معامله»**: برگهٔ دستی BUY/SELL، SL/TP، پیش‌نمایش و تأیید، ریسک هر معامله و کل تا ۵٪، سقف ارزش فرضی ۳× موجودی، قیمت تازه و یک پوزیشن باز در هر نماد. سیگنال‌های موتور پس از تأیید ۹/۹ با دکمه یا با **سوییچ خودکار کاغذیِ پیش‌فرض خاموش + سرویس پایش** باز می‌شوند؛ ثبت Atomic و جلوگیری از تکرار همان کندل؛ ژورنال زمان ورود/خروج و شاهد AI را نشان می‌دهد. خطوط چارت فقط طرح سیگنال‌اند و معامله نیستند | قیمت بازار به معنی اجرای سفارش نیست؛ حجم کسری paper ممکن است زیر حداقل لات/گام بروکر باشد و اسپرد، کارمزد، لغزش و مارجین تأیید نشده‌اند. شورت اسپات Nobitex وجود ندارد؛ بدون اتصال مشتقه/مارجین حق ادعای اجرای شورت نداریم. |
| دانلود رایگان دادهٔ تاریخی | **دکمهٔ مستقیم در «یادگیری» + ذخیرهٔ CSV**: EUR/USD و EUR/GBP نرخ مرجع روزانهٔ ECB از Frankfurter و میانگین ماهانهٔ طلای جهانی از DataHub/World Bank بدون کلید؛ AAPL/MSFT کندل روزانهٔ Twelve Data با کلید خواندنی رایگان کاربر؛ اسپات روزانهٔ XAU/USD فقط اگر پلن Commodities دسترسی دهد؛ بازبینی هویت/زمان/ساختار و حذف روز ناتمام | نرخ ECB و میانگین ماهانهٔ طلا کندل OHLC نیستند؛ سهم نیازمند کلید رایگان ناشر و اسپات روزانهٔ طلا ممکن است نیازمند پلن پولی باشد. داده فقط برای پژوهش است، نه تأیید قیمت زنده یا سفارش. Yahoo (ممنوعیت برداشت خودکار) و Stooq (کپچا) دور زده نمی‌شوند. |
| تاریخچهٔ HistData / MetaTrader | **CSV/ZIP ماهانهٔ پژوهشی HistData XAUUSD/M1** از صفحهٔ رسمی دانلود در مرورگر و فایل انتخابی گوشی: فرمت بدون سربرگ ASCII/MT، BID، EST ثابت UTC−05:00، بررسی ماه/زمان/OHLC/حجم و سقف ۱۲MB؛ بک‌تست فقط ۵۰۰۰ ردیف آخر. واردکنندهٔ CSV/TSV متاتریدر جدا باقی است. | اپ فرم دانلود HistData را دور نمی‌زند؛ فایل را باید از مرورگر بگیرید. نام فایل منشأ را ثابت نمی‌کند. ZIP سالانه، تیک، باینری `.hst`/`.hcc`، اتصال به ترمینال زنده و استفاده در قیمت زنده/سفارش پشتیبانی نمی‌شوند. |
| گزارش ۱۵‌گانه | **۱۸ شاخص** در یادگیری/ژورنال اندروید و خروجی بک‌تست API/وب: تعداد، برد/باخت، Long/Short، نرخ برد، سود/زیان ناخالص و خالص، میانگین برد/باخت، PF، انتظار R، Sharpe معاملاتی، میانگین مدت، دنبالهٔ برد/باخت، بیشینه افت؛ کارمزد هم جداست | Sharpe **به‌ازای معامله، بدون سالانه‌سازی** است؛ Sharpe روزانه نیست. برای معاملات کاغذی، موجودی اولیهٔ فعلی فرض گزارش است (ممکن است طی تاریخچه تغییر کرده باشد). نسبت‌های تعریف‌نشده «—» هستند. |
| بورس ایران و آگاه | **نمای محدود تابلو BrsApi پس از ورود کلید دادهٔ خواندنی کاربر**، توان حقیقی/اسپرد و غربال عددی EPS/P-E؛ TTM دستی از کدال و لینک‌های رسمی آساتریدر | BrsApi واسطهٔ TSETMC است و snapshot فقط ساعت معامله، نه تاریخ مستقل دارد؛ CAN SLIM کامل، اتصال رسمی API آگاه و سفارش واقعی هنوز تأیید نشده‌اند. [جزئیات](WORKSPACES_FA.md). |
| هشدار و شرایط ژورنال | **در APK و UI**: با ورود خودکار روشن، اعلان ۹/۹ فقط **پس از ذخیرهٔ موفق ورود کاغذی**؛ با ورود خودکار خاموش، اعلان برچسب‌دار «فقط کاندیدا». فایل صوتی گوشی با مجوز پایدار/پخش آزمون یا صدای پیش‌فرض اندروید؛ کاندیداها در فایل جدا و شرایط ۹گانهٔ معامله در ژورنال ثبت می‌شوند | در شکست ثبت ژورنال، آلارم ورود/کاندیدای جایگزین نداریم. بدون مدل AI معتبر/فید تازه و ریسک/MTF کافی نه هشدار واجدشرایط داریم و نه ورود خودکار. کاندیدا **معامله نیست**؛ Do Not Disturb، قطع سرویس یا مجوز اعلان مانع صداست؛ صوت روی گوشی واقعی باید تست شود. [راهنمای تست](ALERT_NOBITEX_FA.md). |
| نوبیتکس عمومی/تمرین | **در فضای مستقل «نوبیتکس» و ژورنال مستقل**: غربال فقط‌خواندنی شش نماد در بازارهای اسپات USDT و ریال جداگانه از یک GET آمار رسمی، نمایش همه و علت رد/نامزد پژوهشی، اعتبار ۳ دقیقه‌ایِ زمان دریافت گوشی، و بدون سفارش. افزون بر آن BTCUSDT/BTCIRT کندل، آمار و دفتر سفارش v3 با CSV؛ تمرین spot **فقط BUY کاغذی BTCUSDT** با ask دفتر و خروج bid جدیدتر از ورود، بودجهٔ USDT جدا از XAU/USD | بدون دفتر سفارش معتبر تمرین مسدود و مطالعه باز است. BTCIRT در پاسخ زندهٔ ۲۰۲۶-۰۹-۲۴ اختلاف ۱۰× بین OHLC و آمار/دفتر داشت؛ تا تعیین رسمی واحد، تمرین/ادغام قیمت ممنوع. کارمزد/لغزش/عمق/حداقل سفارش مدل نشده، بین دریافت‌ها SL/TP ممکن است دیده نشود؛ ۹ شرط XAU/AI برای BTC صادق فرض نمی‌شود. [جزئیات](ALERT_NOBITEX_FA.md). |
| Nobitex، MT5، MT5 Bridge، **اجرای خودکار سرور با موجودی واقعی (هدف فاز بعد)** | **فعلاً عمداً غیرفعال**؛ مسیر `/execution/status` و `/execution/preflight` فقط علت رد را نشان می‌دهند؛ POST سفارش همیشه ۵۰۳ است. گام کوچک و امن: `backend/app/services/nobitex_adapter.py` نیمهٔ خواندنیِ قرارداد `VenueAdapter` (`quote`) را با دو اندپوینت عمومی و بدون توکن مستند نوبیتکس (`GET /market/stats`, `GET /v3/orderbook/{symbol}`) پیاده و تست کرده است؛ `instrument_rules`/`submit`/`cancel`/`reconcile` عمداً `NobitexExecutionDisabled` می‌دهند و به هیچ سرویسی wire نشده‌اند — کلید توقف اجرا و آزمون `test_execution_boundary.py` دست‌نخورده می‌مانند. این ردیف مربوط به معماری **سرور** (KMS، idempotency، حسابرسی) است، جدا از ردیف زیر. | برای رسیدن به همان استاندارد سرور (KMS، sandbox رسمی، kill-switch سرور، idempotency پایدار در DB، حسابرسی)، هنوز کاری انجام نشده و این مسیر عمداً بسته می‌ماند. |
| **معاملهٔ واقعی نوبیتکس مستقیم از اندروید (بدون سرور اجرا)** | با درخواست صریح و مکرر کاربر برای وارد کردن کلید API خودش، این مسیر باز شد: `data/NobitexTradingClient.kt` مستقیماً با کلید API شخصیِ کاربر (`Authorization: Token`, ذخیره‌شده فقط روی گوشی در `SettingsStore`) به `POST /market/orders/add|status|update-status` و `POST /users/wallets/balance` وصل می‌شود؛ بخش قرمز «معاملهٔ واقعی نوبیتکس» در فضای نوبیتکس، سقف ارزش هر سفارش (پیش‌فرض ۲۰ USDT، قابل تغییر تا ۵۰۰)، چک‌باکس تأیید صریح و تایپ‌کردن «ارسال» پیش از هر سفارش را الزامی می‌کند؛ دفتر محلی `NobitexLiveTradeStore.kt` کاملاً جدا از ژورنال کاغذی است. | این مسیر معماری سرور بالا (KMS، sandbox رسمی، idempotency سرور، حسابرسی مرکزی) را **ندارد**؛ فقط محافظ‌های سمت کلاینت (سقف مبلغی + تأیید تایپی) را دارد. دقت حجم/قیمت هر بازار در مستندات عمومی نوبیتکس تثبیت نیست و این کد آن را حدس نمی‌زند — رد شدن سفارش توسط خود نوبیتکس (`SmallOrder`, `BadPrice`, ...) عیناً نمایش داده می‌شود. کلید API فقط روی گوشی کاربر ذخیره است؛ از دست رفتن گوشی یعنی از دست رفتن کلید. هیچ حد ضرر/سود خودکاری روی سفارش واقعی ارسال نمی‌شود. |
| **ژورنال Nobitex با «همون متود طلا»** | با درخواست صریح کاربر، `AurumViewModel.nobitexJournalSignal()` دقیقاً همان `SignalEngine` (ایچیموکو+VWAP+EMA200+RSI+ATR+MACD/ADX) طلا را روی کندل‌های واقعی BTC/USDT نوبیتکس اجرا می‌کند و نتیجه را با `openNobitexJournalTrade()` در همان `JournalStore` طلا (`symbol="BTC/USDT"`) ثبت می‌کند؛ همان تسویهٔ کندل بسته، همان آمار win-rate/PF/R، همان سقف کل ریسک ۵٪ حساب (مشترک با طلا). | ورود اینجا **دستی** است، نه خودکار؛ گیت خبر USD طلا (شرط نهم) که مخصوص فارکس است روی BTC اعمال نمی‌شود چون معنی ندارد. جدا از `NobitexPracticeStore` (تمرین درصدی ساده) نگه داشته شده تا هر دو مسیر موجود بمانند. |
| غربالگر رمزارز | **فضای مستقل «کریپتوی جهانی» با نمای عمومی CoinGecko بی‌کلید و سرور پیشرفتهٔ اختیاری**: ۲۰۰ دارایی اول CoinGecko، حداکثر ۱۲ پیش‌گزین، احراز هویت جفتِ دقیق شناسهٔ CoinGecko با Binance spot/USDT، تازگی/عرضه/حجم/اسپرد/توافق قیمت/کندل بسته، برچسب کش و پاک‌شدن نامزدهای منقضی در UI | نیازمند سرور HTTPS و دسترسی واقعی به دو منبع (Demo Key اختیاری CoinGecko فقط روی سرور). خطا = نامعتبر بدون نامزد؛ عبور از فیلتر پیش‌بینی پامپ نیست و دامنه شامل تمام میم‌کوین‌ها/توکن‌های نوپا نیست. هیچ سفارش خودکار/شورت اسپات ندارد. |

## راه‌اندازی الان

1. بک‌اند: `cd backend && python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && cp .env.example .env && uvicorn app.main:app --host 0.0.0.0 --port 8000`.
2. `.env`: برای چارت وب `AURUM_TWELVE_DATA_API_KEY`؛ برای غربال رمزارز بهتر است کلید خواندنی Demo CoinGecko را در `AURUM_COINGECKO_DEMO_API_KEY` فقط روی سرور بگذارید (دسترسی Keyless ممکن است محدود شود). نمایش تیترهای خبر در گوشی از RSS/Atom عمومی ناشرانِ ثابت بدون تنظیم بک‌اند انجام می‌شود؛ گیت/AIِ ورود خبر را بک‌اند به‌طور مستقل از ناشرانِ [راهنمای بک‌اند](../backend/README.md) بررسی می‌کند؛ فید سفارشی دارای مجوز `/news/fa` اختیاری و جداگانه تنظیم می‌شود.
3. اندروید: JDK 17 و Android SDK 35؛ `cd android && ./gradlew testDebugUnitTest assembleDebug`. برای APK تستی، در workflow منتشرشده قدمِ جداگانهٔ JVM هنوز غیرمسدودکننده است، اما `assembleRelease` در Gradle به آزمون‌های `testDebugUnitTest` وابسته است؛ آزمون ناموفق مانع آپلود APK می‌شود. اصلاح خودِ workflow به مجوز `workflows` برای push نیاز دارد. کلید Twelve Data **در APK کامپایل نمی‌شود**؛ در تنظیمات گوشی وارد می‌شود (فقط داده‌خوانی).
4. در تنظیمات **فارکس**، کلید Twelve Data چارت/XAU را وارد کن؛ منابع دیده‌بان ریالی را فقط در «بورس ← ریالی ← منابع» تنظیم کن. برای دیدن تیترهای وب/تقویم/نمای عمومی CoinGecko نیازی به سرور نیست؛ برای گیت خبر/AI فارکس و غربال دو منبعی رمزارز هرکدام نشانی HTTPS مجزای خود را دارند (`https://api.example.com`)؛ آدرس `localhost` گوشی به کامپیوتر توسعه اشاره نمی‌کند. اگر مایل بودی گزینهٔ توقف ورود paper بر مبنای خبر را فعال کن؛ با خوراک ناقص/قدیمی، ورود جدید مسدود می‌شود.
5. خطوط طرح Entry/SL/TP چارت **معامله نیستند**؛ در «ژورنال» معاملهٔ بازشده و کاندیدای هشدار در **دو بخش جدا** هستند. برای ورود خودکار کاغذی ۹/۹ (پیش‌فرض خاموش)، سرورِ دارای کلید AI و اجازهٔ صریح پس از بررسی حقوق ناشر را تنظیم کن، سپس در تنظیمات گوشی پایش و سوییچ خودکار را با تأیید روشن کن. نبود مدل/خبر → نه ورود و نه هشدار واجد شرایط؛ هر خروج فقط روی تیک واقعی ثبت می‌شود. برای هشدار حتی بدون ورود خودکار، پایش و اعلان ۹/۹ را روشن و صدای گوشی را انتخاب/تست کن. در تب «معامله» نیز جهت لانگ یا شورت و SL/TP برای paper دستی ممکن است. بخش نوبیتکس عمومی در فضای مستقل نوبیتکس جدا از غربالگر CoinGecko/Binance است: BTCUSDT/IRT را دریافت/CSV کن، برای BTCUSDT با نرخ ask فقط BUY فرضی Spot باز کن و سرنوشت را در ژورنال **USDT مستقل** ببین؛ BTCIRT به‌علت اختلاف واحد هنوز paper نمی‌شود. راهنمای دقیق: [ALERT_NOBITEX_FA.md](ALERT_NOBITEX_FA.md).
6. برای دادهٔ تاریخی رایگان در «یادگیری» نماد را انتخاب کن، «دریافت داده» و در صورت نیاز «ذخیرهٔ CSV» را بزن. ارزهای EUR/USD و EUR/GBP از ECB و میانگین ماهانهٔ طلا از بانک جهانی بدون کلید می‌آیند؛ برای سهم لینک کلید خواندنی رایگان Twelve Data همان‌جا هست، ولی طلای اسپات روزانه احتمالاً به دسترسی پولی ناشر نیاز دارد. برای متاتریدر هم جداگانه CSV با DATE/TIME/OPEN/HIGH/LOW/CLOSE/TICKVOL و UTC offset سرور بروکر را انتخاب یا URL عمومی HTTPS آن را بده؛ مشخصات و اصالتش را دستی بررسی کن.

## قرارداد و امنیت برای مرحلهٔ سفارش واقعی

- **قبل از پیاده‌سازی**: بازار/جفت Nobitex و نوع حساب/نماد و سرور MT5، مشخصات رسمی API و مجوز دسترسی، حجم حداقل/گام/ارزش هر واحد/کارمزد/اسپرد، محدودیت جغرافیایی/حقوقی و سیاست توقف را تعیین کن. هیچ «کلید عمومی» به معنای مجوز سفارش نیست.
- **سرور اجرا جداگانه**: احراز هویت کوتاه‌عمر و تفکیک کاربر، ذخیرهٔ اسرار در KMS، فقط HTTPS، مجوز حساب، سقف زیان روزانه و اندازهٔ سفارش از سمت سرور، قیمت با timestamp مستقل، گیت خبر با تقویم دارای مجوز، idempotency پایدار در DB، ثبت رویداد و بازیابی وضعیت بروکر پس از قطع شبکه. تأیید دستی و kill-switch قابل آزمون ضروری‌اند.
- **پیش از real money**: تست قرارداد آداپتر با sandbox رسمی، paper/shadow حداقل چند هفته، بررسی تضاد قیمت و توقف خبر در شرایط خطا، آزمون «پاسخ سفارش نامعلوم/تلاش مجدد» و حسابرسی. اجرای واقعی نباید با یک env switch یا گزینه در APK روشن شود.
- این نسخهٔ APK با کلید **debug** امضا می‌شود و فقط برای آزمایش است؛ برای توزیع عمومی signing/keystore امن، حساب کاربر، سیاست حریم خصوصی، محدودسازی نرخ، مانیتورینگ و بررسی حقوقی لازم است. کلید خواندنی Twelve Data فعلاً در فضای خصوصی اپ ذخیره می‌شود و نسخه‌پشتیبان اندروید غیرفعال است؛ برای محصول تجاری حتی این کلید باید به Android Keystore یا پروکسی سرور امن منتقل شود.
